[workspace]
resolver = "2"
members = ["cache", "core", "cpu", "desktop", "executor", "font", "terminal-poc"]

[workspace.package]
license = "MIT"

[workspace.dependencies]
blit = { path = "core" }
blit-executor = { path = "executor" }
blit-font = { path = "font" }
