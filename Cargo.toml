[workspace]
resolver = "2"
members = ["cache", "core", "cpu", "desktop", "executor", "font"]

[workspace.dependencies]
blit = { path = "core" }
blit-executor = { path = "executor" }
blit-font = { path = "font" }
